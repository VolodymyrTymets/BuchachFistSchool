@model BushachFirstSchool.Models.PupilModel.PupilListViewModel
@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<div class="col-md-12 shadow-z-2" style="background-color:white">
    <h2>Учні</h2>
    <div class="row">
        <div class="col-lg-4">
            <ul class="breadcrumb">
                <li>
                    @Html.ActionLink("Класи", "Index", "SchoolClass")
                </li>
                <li class="active">

                    <b>@Model.ClassNme</b>
                </li>
            </ul>
        </div>
    </div>
    <div class="row">
        @{Html.RenderPartial("_create", @Model.PupilCreateViewModel);}
        <div class="col-md-4 col-md-offset-4">
            @{Html.RenderPartial("_loading");}
        </div>
    </div>

    @Html.Action("getPupilsData")
</div>

